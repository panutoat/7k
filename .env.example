# PostgreSQL connection string used by the API routes.
# Local example:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/7k_combo

# Set to "true" if your Postgres requires SSL (e.g. Neon, Supabase, Railway).
PGSSL=false
